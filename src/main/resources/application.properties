## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/yash
spring.datasource.username=yashgoyal
spring.datasource.password=yashgoyal

# create and drop table, good for testing, production set to none or comment it
spring.jpa.hibernate.ddl-auto=create
#-drop
#spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.kafka.order.bootstrap-servers = localhost:9092
spring.kafka.order.topic.customer-producer = customer-events

spring.kafka.producer.properties[max.block.ms]=10000
